import { DomainGroupDashboardDto } from './domain-group-dashboard-dto';

describe('DomainGroupDashboardDto', () => {
  it('should create an instance', () => {
    expect(new DomainGroupDashboardDto()).toBeTruthy();
  });
});
